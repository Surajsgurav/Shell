pipeline {
    agent any
    stages {
      stage('run shell script') {

            steps {
                   sh 'cd /home/ec2-user/shell'
                   sh './hello.sh'
                  }
              }
             }
           }

